#define W_CONCAT(x,y) W_CONCAT_HELPER(x,y)
#define W_CONCAT_HELPER(x,y) W_CONCAT_HELPER2(x,y)
#define W_CONCAT_HELPER2(x,y) x##y
#define W_STRINGIZE(str) W_STRINGIZE_HELPER(str)
#define W_STRINGIZE_HELPER(x) #x
#define W_COMPILE_TIME_MAX(a, b) ((a) > (b) ? (a) : (b))
#define W_COMPILE_TIME_MIN(a, b) ((a) < (b) ? (a) : (b))
#define W_BIT(n) (1U << (n))
#pragma once